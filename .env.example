# Base URL of the backend API. Since the server is reached only by IP (no
# domain), this is the one line to update when deploying - e.g.
# http://203.0.113.10:8000/api - and later to a domain if one is added.
VITE_API_BASE_URL=http://localhost:8000/api
